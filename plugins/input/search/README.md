# Search / From plugins (legacy location)

The from-plugins that were previously housed here have moved to [`plugins/from/`](../../from/).

This directory now only contains:

| Plugin | Description |
|--------|-------------|
| [jackett_input](jackett/) | Fetch recent results from Jackett indexers as a passive feed (PhaseInput) |

The search/from plugins (`jackett`, `rss_search`, `trakt_list`, `tvdb_favorites`) are now in [`plugins/from/`](../../from/). See the [`plugins/from/README.md`](../../from/README.md) for documentation.
